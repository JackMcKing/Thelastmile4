<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andrew.thelastmile">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SupermarketLoginActivity"
            android:label="@string/title_activity_supermarket_login" />
        <activity
            android:name=".userLoginActivity"
            android:label="@string/title_activity_user_login" />
        <activity
            android:name=".Singup"
            android:label="@string/title_activity_singup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Courier"
            android:label="@string/title_activity_courier"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".User"
            android:label="@string/title_activity_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".supermaret"
            android:label="@string/title_activity_supermaret"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".supermarket"
            android:label="@string/title_activity_supermarket"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".listlayout"
            android:label="@string/title_activity_listlayout"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>
